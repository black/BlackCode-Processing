

Select a recorded interaction session (the .xstream file the 
eye tracking app in your Browser emitted into the path that was 
specified in its config) in you session directory, or a ZIP of 
that session directory. Give the replay some time to load (small 
sessions load in ~1s, big sessions take up to ten seconds).

