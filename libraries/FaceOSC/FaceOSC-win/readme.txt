To edit the settings for FaceOSC:

In OSX:
Right click on the app and select "Show Package Contents". Inside the package, navigate to Contents/Resources/data/ and you'll find settings.xml. Further instructions are contained within the settings.xml file.

In Windows/Linux:
Open settings.xml. Further instructions are contained within the settings.xml file.